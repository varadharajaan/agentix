# Required — get one at https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# Optional overrides
OPENAI_CHAT_MODEL=gpt-5.5
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Where uploaded repos + the SQLite DB are stored on disk
DATA_DIR=./data
