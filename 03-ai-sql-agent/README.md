# AI SQL Agent

AI SQL Agent is a Next.js application that lets users select or upload a SQLite database, ask questions in plain English, and receive a safe, AI-generated SQL query, its results, and a concise data-backed explanation.

For setup, architecture, API, agent workflow, safety details, and project structure, refer to the [`docs/`](./docs/) folder.
