# Required: your OpenAI API key
OPENAI_API_KEY=sk-...

# Optional: override the chat/extraction model (must support tool calling)
MEMORY_MODEL=gpt-5.1

# Optional: override the embedding model used for semantic memory retrieval
MEMORY_EMBEDDING_MODEL=text-embedding-3-small
