# OpenAI API key — used for GPT-5.5 chat completions and
# text-embedding-3-small embeddings.
OPENAI_API_KEY=sk-...

# Optional: override the chat model (defaults to gpt-5.5).
OPENAI_CHAT_MODEL=gpt-5.5


